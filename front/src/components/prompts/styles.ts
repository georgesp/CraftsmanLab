import { styled } from '@mui/material/styles';
import { Box } from '@mui/material';
import { TYPOGRAPHY } from '../../styles/typography';

// This file can be removed entirely if no other styled components are needed
// CodeBlock is now handled by the dedicated component in ui/CodeBlock
